#ifndef ENUMS_CHOICES_DEF_H
#define ENUMS_CHOICES_DEF_H

namespace enums {
namespace ChoicesDef {
enum Choices {
	NONE = -1,

	FIRST_CUSTOM = 0,
};
}
}
DECLARE_ENUM(enums::ChoicesDef::Choices);

#endif // ENUMS_CHOICES_DEF_H
