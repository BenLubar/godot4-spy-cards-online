#ifndef ENUMS_CHARACTER_DEF_H
#define ENUMS_CHARACTER_DEF_H

namespace enums {
namespace CharacterDef {
enum Character {
	NONE = -1,

	FIRST_CUSTOM = 0,
};
}
}
DECLARE_ENUM(enums::CharacterDef::Character);

#endif // ENUMS_CHARACTER_DEF_H
